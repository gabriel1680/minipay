plugins {
    id 'java-conventions'
    id 'java-library'
}

group = 'com.minipay.domain'

dependencies {
    testCompileOnly 'org.projectlombok:lombok:1.18.32'
}

configurations {
    testClasses {
        extendsFrom(testImplementation)
    }
}

tasks.register('testJar', Jar) {
    archiveClassifier.set('test')
    from sourceSets.test.output
}

artifacts {
    testClasses testJar
}